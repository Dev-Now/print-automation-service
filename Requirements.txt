# Auto-Print Service - Python Dependencies
# Brother MFC-L2750DW Automated Printer Management
# Python 3.11+

# Windows API Integration
pywin32>=306              # Windows APIs for printing, services, WiFi

# WiFi Management
pywifi>=1.1.12           # WiFi control and connection management
comtypes>=1.2.0          # Required for pywifi on Windows

# File System Monitoring
watchdog>=3.0.0          # Monitor print_jobs directory for new files

# Document Conversion via Gotenberg
# NOTE: Gotenberg Docker container is required for DOCX to PDF conversion
# Run: docker run -d -p 3000:3000 --name gotenberg gotenberg/gotenberg:8
# See: https://gotenberg.dev/docs/getting-started/introduction

# HTTP Client for Gotenberg API
requests>=2.31.0         # HTTP requests for Gotenberg API calls

# Configuration & Logging
python-json-logger>=2.0.7  # JSON formatted logging (optional)

# Utility Libraries
pillow>=10.1.0          # Image processing (future color support)

# Development & Testing (optional)
pytest>=7.4.3           # Unit testing framework
pytest-cov>=4.1.0       # Test coverage reporting
